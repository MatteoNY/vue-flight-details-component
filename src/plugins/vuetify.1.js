import Vue from 'vue'
import Vuetify, { VApp, VDialog, VIcon, VBtn, VContainer, VFlex, VLayout } from 'vuetify/lib'
import 'vuetify/src/stylus/app.styl'

Vue.use(Vuetify, {
  VApp, VDialog, VIcon, VBtn, VContainer, VFlex, VLayout,
  iconfont: 'fa',
})
